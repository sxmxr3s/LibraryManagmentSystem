package libraryManagementSystem;

/**
 * This class contains the information about a PrintedBook such 
 * as its page number, etc. It also contains the methods to borrow 
 * and return the book.
 * 
 * @author samar
 * @author dayanna
 * @author chanel
 */
public class PrintedBook extends Book implements Borrowable {
	private String format;
	private int pages;

	/**
	 * Initializes the format and pages before getting the ISBN,
	 * title, author, genre, and availability from the superclass Book.
	 * 
	 * @param isbn ISBN code.
	 * @param title Title of book.
	 * @param author Author of book.
	 * @param genre Genre of book.
	 * @param available The book's availability.
	 * @param format The format of the book.
	 * @param pages The number of pages in the book.
	 */
	public PrintedBook(String isbn, String title, String author, 
			Genre genre, boolean available, String format, int pages) {
		super(isbn, title, author, genre, available);
		this.format = format;
		this.pages = pages;
	}

	/**
	 * Allows the user to borrow the book if available.
	 */
	public void borrowBook() {
		
	}

	/**
	 * Allows the user to return the book and change it availability.
	 */
	public void returnBook() {

	}
}
